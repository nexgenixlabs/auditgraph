import Resources from './Resources';

export default function KeyVaultSecurity() {
  return (
    <Resources
      lockedType="key_vault"
      pageTitle="Key Vault Security"
      pageSubtitle="Security configuration audit for Azure Key Vaults"
    />
  );
}
