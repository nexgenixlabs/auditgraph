import Resources from './Resources';

export default function StorageSecurity() {
  return (
    <Resources
      lockedType="storage_account"
      pageTitle="Storage Account Security"
      pageSubtitle="Security configuration audit for Azure Storage Accounts"
    />
  );
}
