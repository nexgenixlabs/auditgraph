# Dependabot disabled — dependency updates are managed manually via Jira stories.
# All PRs require a Jira Story ID (AG-nnn) in the title, which dependabot cannot provide.
# Re-enable when a Jira-integrated dependency update process is in place.
version: 2
updates: []
