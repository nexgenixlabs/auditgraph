# AG-132: Cross-Tenant Leak Fix — AI Copilot

**Date**: 2026-04-28
**Ticket**: AG-132 (CRITICAL HOTFIX)
**CWE**: CWE-639, CWE-284, CWE-200
**OWASP**: A01:2021, API1:2023
**NIST**: SP 800-53 AC-3, AC-4, AC-6
**MITRE**: ATT&CK T1530

---

## Sweep Result

### AG-129 audit reported: 4 sites
### Lint --strict found: **10 violations** (6 more than the audit reported)

The AG-129 audit only caught queries referencing `identities` and `discovery_runs`.
The lint additionally caught `anomalies` (2 sites) and `drift_reports` (1 site) queries
that were also unscoped — plus 4 `identities` sites that the lint only detected after
adding `identities` to the `TENANT_TABLES` set.

| # | File | Line | Function | Table | Status |
|---|------|------|----------|-------|--------|
| 1 | copilot_service.py | 190 | gather_context | discovery_runs | FIXED |
| 2 | copilot_service.py | 209 | gather_context | anomalies | FIXED |
| 3 | copilot_service.py | 225 | gather_context | identities | FIXED |
| 4 | copilot_service.py | 240 | gather_context | identities | FIXED |
| 5 | copilot_service.py | 251 | gather_context | drift_reports | FIXED |
| 6 | copilot_service.py | 286 | get_suggestions | anomalies | FIXED |
| 7 | copilot_service.py | 292 | get_suggestions | identities | FIXED |
| 8 | copilot_service.py | 292 | get_suggestions | discovery_runs | FIXED |
| 9 | copilot_service.py | 303 | get_suggestions | identities | FIXED |
| 10 | copilot_service.py | 303 | get_suggestions | discovery_runs | FIXED |

---

## Lint Transcripts

### BEFORE Fix
```
$ python3 scripts/lint_tenant_scope.py app/services/copilot_service.py

========================================================================
TENANT-SCOPE VIOLATIONS: 10 found
========================================================================

  app/services/copilot_service.py:190 [gather_context] table=discovery_runs: missing organization_id filter
  app/services/copilot_service.py:209 [gather_context] table=anomalies: missing organization_id filter
  app/services/copilot_service.py:225 [gather_context] table=identities: missing organization_id filter
  app/services/copilot_service.py:240 [gather_context] table=identities: missing organization_id filter
  app/services/copilot_service.py:251 [gather_context] table=drift_reports: missing organization_id filter
  app/services/copilot_service.py:286 [get_suggestions] table=anomalies: missing organization_id filter
  app/services/copilot_service.py:292 [get_suggestions] table=identities: missing organization_id filter
  app/services/copilot_service.py:292 [get_suggestions] table=discovery_runs: missing organization_id filter
  app/services/copilot_service.py:303 [get_suggestions] table=identities: missing organization_id filter
  app/services/copilot_service.py:303 [get_suggestions] table=discovery_runs: missing organization_id filter
```

### AFTER Fix
```
$ python3 scripts/lint_tenant_scope.py app/services/copilot_service.py
tenant-scope lint: OK (no violations)
```

---

## Files Changed

| File | Change |
|------|--------|
| `app/services/copilot_service.py` | `@requires_org_id` on gather_context, get_suggestions, ask; all SQL filtered by `organization_id`; LLM context fingerprint log |
| `app/api/handlers.py` | Pass `org_id=org_id` to all copilot service calls (4 sites) |
| `app/security/tenant_scope.py` | Added `'identities'` to `_BASELINE_TENANT_TABLES` |
| `scripts/lint_tenant_scope.py` | Added `'identities'` to `TENANT_TABLES` |
| `.sql-lint-baseline.json` | Removed 6 fixed copilot_service entries (738 → 732) |
| `tests/security/test_copilot_tenant_isolation.py` | **NEW** — 16 tests |
| `tests/security/test_tenant_scope.py` | Updated baseline assertion (identities now in set) |
| `docs/verification/AG-132_fix_20260428.md` | **THIS FILE** |

---

## Test Results

### New Tests (AG-132)
```
TestGatherContextTenantIsolation   — 7 tests PASSED
TestGetSuggestionsTenantIsolation  — 3 tests PASSED
TestAskTenantIsolation             — 3 tests PASSED
TestNoAdminFallback                — 1 test  PASSED
TestLlmPromptInspection            — 2 tests PASSED
                                   ───────────────
                                     16 new tests
```

### Full Security Suite
```
$ python3 -m pytest tests/security/
224 passed in 2.42s
```

---

## LLM-Prompt Inspection Transcript

```python
# TestLlmPromptInspection::test_llm_prompt_never_contains_other_org_data
#
# Calls ask() as org=2. Captures system prompt sent to LLM.
# Asserts NONE of these org=3 fingerprints appear:
#   'Eve@globex', 'Run #200', '100 identities',
#   'guest: 50', 'managed_identity_system: 50'
# RESULT: PASSED — zero cross-tenant data in LLM prompt

# TestLlmPromptInspection::test_llm_prompt_contains_correct_org_data
#
# Calls ask() as org=2. Verifies 'Run #100' or '50 identities' IS present.
# RESULT: PASSED — correct org data reaches LLM
```

---

## Admin Fallback Verification

```
$ grep -n "if org_id is None\|default.*org\|org_id.*=.*None\|Optional.*org_id" app/services/copilot_service.py
(empty — no results)
```

No silent admin-fallback path exists.

---

## Compliance Mapping

| Standard | Reference | Control |
|----------|-----------|---------|
| CWE-639 | Authorization Bypass Through User-Controlled Key | @requires_org_id decorator on all data methods |
| CWE-284 | Improper Access Control | Belt-and-suspenders: organization_id filter on BOTH joined tables |
| CWE-200 | Exposure of Sensitive Information | LLM prompt contains only post-filter data |
| OWASP A01:2021 | Broken Access Control | No admin fallback; TenantScopeError on missing org_id |
| OWASP API1:2023 | Broken Object Level Authorization | Every SQL query parameterizes org_id |
| NIST SP 800-53 AC-3 | Access Enforcement | @requires_org_id validates at decoration + call time |
| NIST SP 800-53 AC-4 | Information Flow Enforcement | Data never flows cross-tenant in copilot context |
| NIST SP 800-53 AC-6 | Least Privilege | Copilot sees only the authenticated user's org data |
| MITRE ATT&CK T1530 | Data from Cloud Storage | Eliminated — AI copilot can no longer exfiltrate cross-tenant identity data |

---

## Lint Baseline Update

```
BEFORE: 738 entries in .sql-lint-baseline.json
AFTER:  732 entries (removed 6 fixed copilot_service violations)
DELTA:  -6 (improvement)
```

---

## UI Lock Verification

```
$ git diff --stat -- '**/CISODashboard.tsx' '**/IdentityInventory.tsx' '**/IdentityDetail*'
(empty — no UI files modified)
```

---

## Scope Confirmation

- **AG-129 schema work**: NOT touched (no DDL, no migration, no new columns)
- **AG-131 (Redis replay cache)**: NOT touched
- **No new modules created** — reuses AG-94's `app/security/tenant_scope.py`
- **No Optional[int] defaults** — all org_id params are keyword-only with no default

---

## Production-Readiness Checklist

- [x] All 10 lint violations fixed (more than the audit's 4)
- [x] @requires_org_id on gather_context, get_suggestions, ask
- [x] Every SQL query has explicit organization_id = %s filter
- [x] No silent admin-fallback path (grep confirms)
- [x] LLM prompts contain only post-filter data (mock test proves)
- [x] LLM-input fingerprint log emitted (copilot_context_gathered event)
- [x] Cross-tenant isolation test green (16 tests)
- [x] Negative tests green (TenantScopeError on missing/invalid org_id)
- [x] Full security suite: 224 passed
- [x] lint_tenant_scope.py --strict on copilot_service: 0 violations
- [x] Baseline count decreased (738 → 732, delta: -6)
- [x] UI files untouched
- [x] NO schema changes
- [x] NO new modules — reuses AG-94's tenant_scope.py
- [x] `identities` added to TENANT_TABLES in both lint and tenant_scope.py
