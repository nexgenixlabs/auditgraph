# AG-95-v2: Fix Verification Report

**Date**: 2026-04-28
**Ticket**: AG-95-v2 (closes 4 gaps from AG-95 v1 review)
**Implementer**: Claude

---

## Gaps Addressed

| # | Gap | CWE | Status |
|---|-----|-----|--------|
| 1 | Encrypt NameID and assertions by default | CWE-311, OWASP A02:2021 | FIXED |
| 2 | Boot-time _assert_secure() invocation | CWE-345 | FIXED |
| 3 | Lint rule catching False encryption literals | — | FIXED |
| 4 | Tests proving each gap is closed | — | FIXED |

---

## BEFORE Transcripts

### Grep: Encryption False Literals (BEFORE fix)
```
$ grep -rn "wantNameIdEncrypted.*False" app/ --include="*.py"
app/security/sso_security.py:90:    'wantNameIdEncrypted': False,  # Many IdPs don't support encryption
app/security/sso_security.py:162:            'wantNameIdEncrypted': False,
app/api/saml.py:94:                'wantNameIdEncrypted': False,

$ grep -rn "wantAssertionsEncrypted.*False" app/ --include="*.py"
app/security/sso_security.py:91:    'wantAssertionsEncrypted': False,  # Requires SP private key for decryption
app/security/sso_security.py:163:            'wantAssertionsEncrypted': False,
```
**5 False literals total across 2 files.**

### Grep: Boot-time Validation (BEFORE fix)
```
$ grep -rn "validate_saml_settings_at_boot" app/main.py
(empty — no results)
```

### Lint --strict for SSO006/SSO007 (BEFORE fix — simulated)
The lint rules SSO006/SSO007 did not exist in v1. After adding them but before
fixing the code, running `--strict --rules SSO006,SSO007` would have caught
all 5 False literals.

---

## AFTER Transcripts

### Grep: Encryption False Literals (AFTER fix)
```
$ grep -rn "wantNameIdEncrypted.*False" app/ --include="*.py"
(empty — 0 results)

$ grep -rn "wantAssertionsEncrypted.*False" app/ --include="*.py"
(empty — 0 results)
```
**0 False literals remaining in app/.**

### Grep: Boot-time Validation (AFTER fix)
```
$ grep -n "validate_saml_settings_at_boot" app/main.py
1098:        from app.security.sso_security import validate_saml_settings_at_boot
1099:        validate_saml_settings_at_boot()

$ grep -n "BootValidationError" app/main.py
1101:        from app.security.sso_security import BootValidationError
1102:        if isinstance(_boot_err, BootValidationError):
```

### Lint --strict (AFTER fix)
```
$ python3 scripts/lint_sso_settings.py --strict --rules SSO006,SSO007,SSO008
SSO Lint [STRICT]: OK — 0 violations (rules: SSO006,SSO007,SSO008)
```

---

## Test Results

### New Test Classes (AG-95-v2)
```
TestEncryptionDefaults        — 12 tests PASSED
TestBootValidation            —  8 tests PASSED
TestLintCatchesFalseLiterals  —  5 tests PASSED
                              ─────────────────
                                25 new tests
```

### Full AG-95 Test Suite (including prior 64 tests)
```
$ python3 -m pytest tests/security/test_sso_security.py
89 passed in 1.48s
```

### Full Security Test Suite (AG-94 + AG-95 + AG-95-v2)
```
$ python3 -m pytest tests/security/
207 passed in 2.33s
```

---

## Files Modified

| File | Change |
|------|--------|
| `app/security/sso_security.py` | Encryption defaults True, per-org override, BootValidationError, validate_saml_settings_at_boot(), _do_boot_validation(), _ENCRYPTION_FLAGS, SSO_ENCRYPTION_OVERRIDE_KEYS |
| `app/api/saml.py` | Fallback encryption True, org_settings passthrough |
| `app/api/handlers.py` | Load org_settings in saml_login/saml_acs, superadmin guard on encryption override in save_sso_settings |
| `app/main.py` | Boot-time validation wired after DDL, before serving traffic |
| `scripts/lint_sso_settings.py` | SSO006/SSO007/SSO008 rules, --strict mode, --rules filter |
| `tests/security/test_sso_security.py` | 25 new tests across 3 classes |
| `docs/security/sso-saml-security.md` | Encryption section, IdP setup, compliance table update |
| `docs/runbooks/saml-incident-response.md` | SP cert rotation procedure |

---

## Compliance Update

| Standard | Reference | Control |
|----------|-----------|---------|
| CWE-311 | Missing Encryption of Sensitive Data | wantNameIdEncrypted=True (default) |
| OWASP A02:2021 | Cryptographic Failures | Encryption defaults enforced |
| SAML V2.0 §6.2 | Assertion Encryption | wantAssertionsEncrypted=True (default) |
| SAML V2.0 §3.4 | NameID Confidentiality | wantNameIdEncrypted=True (default) |
| NIST SP 800-63B | Federation encryption requirements | Encrypted by default, auditable override |

---

## Scope Exclusion

**AG-131 (Redis-backed replay cache)** is NOT touched in this fix and remains
tracked separately. The in-memory `AssertionReplayCache` and `AuthnRequestCache`
are unchanged.

---

## UI Lock Verification
```
$ git diff --stat -- '**/CISODashboard.tsx' '**/IdentityInventory.tsx' '**/IdentityDetail*'
(empty — no UI files modified)
```

---

## Production-Readiness Checklist

- [x] Grep returns 0 hits for `wantNameIdEncrypted: False` outside tests
- [x] Grep returns 0 hits for `wantAssertionsEncrypted: False` outside tests
- [x] `validate_saml_settings_at_boot` wired into app factory and called before serve
- [x] Settings hash log emitted at every startup (`saml_settings_baseline`)
- [x] Lint `--strict` for SSO006/SSO007/SSO008 reports 0 violations
- [x] All 3 new test classes green (25 tests)
- [x] Prior 64 AG-95 tests still green (no regressions)
- [x] Full security suite: 207 passed
- [x] sso-saml-security.md updated (encryption defaults, per-org override, IdP setup, boot validation)
- [x] saml-incident-response.md updated (SP encryption cert rotation)
- [x] CISODashboard.tsx, IdentityInventory.tsx, Identity Detail tabs not modified
- [x] AG-131 (Redis replay cache) NOT touched
