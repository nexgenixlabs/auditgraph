# AG-94 Fix Verification — Cross-Tenant Data Leak Elimination

**Date**: 2026-04-27
**Severity**: CVSS v3.1 ≈ 8.6 (HIGH–CRITICAL)
**CWE**: CWE-639 (Authorization Bypass), CWE-284 (Improper Access Control)
**OWASP**: A01:2021 — Broken Access Control

## Audit Summary

| Metric | Count |
|--------|-------|
| Tenant-scoped tables | 49 |
| Total SQL query sites scanned | ~1,030 |
| LEAK sites identified | 14 |
| REVIEW sites (intentional cross-org) | 27 |
| SAFE sites (already scoped) | ~989 |
| All LEAK sites fixed | Yes |

## LEAK Sites Fixed

| # | File | Method | Table | Fix |
|---|------|--------|-------|-----|
| 1 | database.py:3185 | `get_latest_discovery_run()` | discovery_runs | Added `org_id: int` kwarg + `WHERE organization_id = %s` |
| 2 | database.py:4271 | `get_report_data()` | discovery_runs | Added `org_id: int` kwarg + org filter on all 3 sub-queries |
| 3 | database.py:5410 | `get_role_usage_stats()` | discovery_runs | Added `org_id: int` kwarg + `WHERE organization_id = %s` on MAX(id) |
| 4 | database.py:8932 | `populate_campaign_reviews()` | discovery_runs | Added `org_id: int` kwarg + `WHERE organization_id = %s` on MAX(id) |
| 5 | database.py:9375 | `_get_group_risk_stats()` | discovery_runs | Added org_id param + scoped MAX(id) |
| 6 | database.py:9418 | `get_groups()` | discovery_runs | Added org_id param + scoped MAX(id) |
| 7 | database.py:9452 | `get_group()` | discovery_runs | Added org_id param + scoped MAX(id) |
| 8 | database.py:9613 | `get_group_comparison()` | discovery_runs | Added org_id param + scoped MAX(id) |
| 9 | database.py:9670 | `get_identity_groups()` | discovery_runs | Added org_id param + scoped MAX(id) |
| 10 | database.py:10470 | `get_soar_actions()` | discovery_runs | Scoped subquery JOIN by organization_id |
| 11 | database.py:10512 | `get_soar_action_stats()` | discovery_runs | Scoped both subquery JOINs by organization_id |
| 12 | database.py:12013 | `get_active_snapshot_job()` | snapshot_jobs | Added `org_id` param + `WHERE organization_id = %s` |
| 13 | database.py:26079 | `get_security_summary_context()` | anomalies | Added `WHERE organization_id = %s` to anomalies count |
| 14 | azure_discovery.py:6106 | `_load_ice_config()` | settings | Changed `Database()` → `Database(organization_id=self.db_org_id)` + org filter |

## Files Changed

| File | Changes |
|------|---------|
| `backend/app/security/tenant_scope.py` | **NEW** — @requires_org_id, @cross_org, tenant table registry |
| `backend/app/database.py` | 14 method signatures updated, org_id filters added |
| `backend/app/api/handlers.py` | 6 caller sites updated to pass `org_id=_org_id()` |
| `backend/app/services/email_service.py` | 1 caller updated |
| `backend/app/scheduler.py` | 1 caller updated |
| `backend/app/engines/discovery/azure_discovery.py` | 1 method fixed (admin → org-scoped) |
| `backend/tests/security/test_tenant_scope.py` | **NEW** — 28 tests |
| `backend/tests/security/test_cross_tenant_isolation.py` | **NEW** — 34 tests |
| `backend/scripts/lint_tenant_scope.py` | **NEW** — static lint for future violations |
| `docs/security/tenant-isolation.md` | **NEW** — security architecture doc |
| `docs/security/tenant_tables.txt` | **NEW** — authoritative tenant table list |

## Test Results

```
62 passed in 0.75s

tests/security/test_tenant_scope.py — 28 tests
  TestRequiresOrgId — 12 tests (missing, None, zero, negative, non-int, valid, decoration checks)
  TestCrossOrg — 5 tests (reason, audit_event, audit log, attributes)
  TestTenantTableRegistry — 5 tests (baseline, classification, cache)
  TestValidateOrgId — 6 tests (None, zero, negative, string, valid, logging)

tests/security/test_cross_tenant_isolation.py — 34 tests
  TestSignatureEnforcement — 9 tests (every refactored method has org_id param)
  TestSqlContainsOrgFilter — 16 tests (every method's SQL contains organization_id)
  TestNoUnscopedMaxPattern — 6 tests (dangerous MAX(id) pattern eliminated)
  TestHandlerWiring — 5 tests (handlers pass org_id to Database methods)
  TestAzureDiscoveryIceConfig — 1 test (org-scoped connection)
```

## Lint Script

```
$ python scripts/lint_tenant_scope.py
tenant-scope lint: OK (no violations)
```

Baseline: 738 existing patterns (RLS-protected, not application-layer filtered) tracked in `.sql-lint-baseline.json`. New queries on tenant tables without org_id filter will fail the lint.

## Compliance Mapping

| Standard | Control | Artifact |
|----------|---------|----------|
| CWE-639 | Authorization Bypass | `@requires_org_id` — fails closed on missing org_id |
| CWE-284 | Improper Access Control | All 14 leak sites fixed with explicit org_id filter |
| OWASP A01 | Broken Access Control | `tenant_scope.py` module — centralized enforcement |
| NIST AC-3 | Access Enforcement | Application + RLS dual enforcement |
| NIST AC-4 | Information Flow | `WHERE organization_id = %s` on every tenant query |
| NIST AC-6 | Least Privilege | `@cross_org` escape hatch with audit trail |
| NIST AU-2 | Audit Events | `TenantScopeError` WARN + `@cross_org` INFO logs |
| CIS 3.3 | Data Access Control | `_BASELINE_TENANT_TABLES` registry |
| CIS 6.8 | Role-Based Access | JWT org_id → `_org_id()` → Database method |
| MITRE T1078.004 | Cloud Accounts | Auth middleware extracts org from JWT, not request |
| MITRE T1530 | Data from Storage | org_id parameter prevents cross-tenant reads |

## Residual Items

| Item | Status | Notes |
|------|--------|-------|
| 738 baseline entries | REVIEW | Queries on tenant tables via RLS-scoped connections — safe but not application-filtered. Track for incremental hardening. |
| 27 REVIEW sites | DOCUMENTED | Intentionally cross-org (admin endpoints, integrity checks, scripts). All gated by superadmin role or manual execution. |
| `identities` table | N/A | No `organization_id` column — scoped via `discovery_run_id` FK to org-scoped `discovery_runs`. Architecture decision. |
